# 사용 순서

## 1. `run.bat` 실행
`D:\youtube_to_mp4\run.bat`을 더블클릭한다.

- 백엔드(uvicorn) 창과 터널(cloudflared) 창, 총 2개 창이 뜬다.
- 사용하는 동안 두 창을 닫지 않고 그대로 둔다.

## 2. 새 터널 주소 확인 및 반영
`run.bat` 실행 화면에 새로 발급된 백엔드 주소가 표시된다.

- **이전에 쓰던 주소와 다르면** (Quick Tunnel 특성상 거의 매번 바뀜), 화면에 안내된 대로 아래 명령을 실행한다:
  ```bash
  git add frontend/config.js
  git commit -m "update backend tunnel url"
  git push
  ```
- 주소가 같으면 이 단계는 건너뛴다.
- push 후 GitHub Pages가 자동으로 재배포된다 (몇십 초 소요).

## 3. 웹앱 접속
PC나 모바일 어디서든 아래 주소로 접속한다:

https://luckyyyj77-wq.github.io/youtube-to-mp4/

## 종료할 때
`run.bat`이 띄운 백엔드 창과 터널 창을 닫으면 서비스가 중단된다.

---

### 참고
- 최초 1회만 필요한 준비물: ffmpeg 설치, `winget install --id Cloudflare.cloudflared`로 cloudflared 설치 (둘 다 이미 설치되어 있다면 생략)
- 매번 git push 하는 게 번거로우면, Cloudflare 계정 + 도메인으로 고정 주소를 설정해 이 단계를 없앨 수 있음 (미적용 상태, 필요 시 요청)
